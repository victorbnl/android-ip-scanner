<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AndroidNetworkScanner" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/accentDark</item>
        <!-- Customize your theme here. -->
        <item name="android:colorAccent">@color/accent</item>
    </style>
</resources>